# FlowHub 产品说明

## 1. 这是什么

FlowHub 是一个面向 OpenClaw 生态的对话式自动化平台。

它的核心体验不是“找 Skill、配流程、点很多按钮”，而是：

- 用户直接在聊天窗口里说一句话
- 平台自动理解需求
- 自动从 Skill 索引目录中找到合适能力
- 自动组合成工作流
- 把工作流方案和使用方式直接回复到同一个聊天窗口

一句话概括：

**用户只负责提需求，FlowHub 负责找 Skill、拼流程、给结果。**

## 2. 解决什么问题

OpenClaw 生态本身能力很强，但对普通用户来说门槛偏高，主要问题包括：

- 不知道有哪些 Skill
- 不知道应该选哪个 Skill
- 不知道多个 Skill 应该怎么组合
- 不知道如何把技术结果变成用户能理解的回复

FlowHub 的作用就是把这些复杂过程藏起来，让用户只面对“聊天式提需求”。

## 3. 适合谁用

FlowHub 适合以下用户：

- 不熟悉 OpenClaw 的新手
- 不想自己配置工作流的运营或业务人员
- 需要快速把多 Skill 能力组合起来的团队
- 希望在聊天中直接完成自动化请求和确认的场景

## 4. 核心能力

### 4.1 一句话生成方案

用户只需要在 OpenClaw 对话框里输入一句命令，例如：

```text
帮我抓取这个 incident API 的最新状态，并生成一段发给客户的 markdown 简报
```

FlowHub 会自动返回：

- 这条命令会怎么执行
- 系统选中了哪些 Skill
- 每个 Skill 的用途
- 用户接下来该怎么确认

### 4.2 自动组合 Skill

FlowHub 会根据用户命令，从本地 Skill 索引里找出最合适的组合，而不是只返回单个工具。

例如：

- 一个 Skill 负责采集数据
- 一个 Skill 负责总结或输出
- 最后再拼成完整 workflow

### 4.3 保持在同一个聊天窗口里完成

用户不需要跳到外部表单页面，也不需要进入后台系统。

整个交互都在 OpenClaw 的聊天窗口里完成：

- 发送命令
- 查看方案
- 确认执行
- 收到反馈

### 4.4 给用户看的不是技术结构，而是可读回复

FlowHub 不只是给出 workflow JSON，还会给出：

- 面向用户的摘要
- Skill 简介
- 使用方式
- 确认提示

所以它既是“工作流规划器”，也是“结果翻译层”。

## 5. 用户使用流程

### 第一步：用户发一条命令

用户在 OpenClaw 聊天里说一句自然语言需求。

### 第二步：系统返回方案

FlowHub 会返回：

- 工作流简介
- 推荐 Skill 列表
- Skill 的用途说明
- 使用方式
- 是否确认执行

### 第三步：用户确认

用户在同一个聊天里继续回复确认。

### 第四步：系统推进执行

FlowHub 把请求推进到待执行状态，并生成可继续发送给用户的确认消息。

## 6. 典型使用场景

### 6.1 数据抓取与摘要

用户需求：

- 抓取网站或 API 数据
- 输出日报、周报、月报

### 6.2 客户沟通场景

用户需求：

- 生成面向客户的状态说明
- 把技术数据转成可读回复

### 6.3 内容整理场景

用户需求：

- 从多个来源收集内容
- 汇总成 markdown、PDF、表格等格式

### 6.4 标准化流程生成

用户需求：

- 一条命令生成可复用 workflow
- 之后重复执行或交给运行端执行

## 7. 现在系统里有哪些组成部分

### 7.1 FlowHub Backend

这是平台的大脑，负责：

- 命令分析
- Skill 检索
- 工作流组合
- 生成回复内容

### 7.2 Skill 索引目录

这是平台的能力来源库。

它会定时同步 ClawHub 公共 Skill 目录，并保存在本地索引中，方便后续组合。

### 7.3 OpenClaw Chat Plugin

这是连接 OpenClaw 聊天和 FlowHub 的桥梁。

作用：

- 把聊天消息转成 FlowHub 请求
- 把 FlowHub 返回内容继续发回聊天窗口

### 7.4 Client Runtime

这是工作流的执行端，负责真正执行 workflow 并回传运行结果。

## 8. 技术栈

从产品视角看，当前技术底座包括：

- 后端：FastAPI + SQLAlchemy + SQLite
- 前端：Next.js + React
- 执行端：Tauri + TypeScript
- OpenClaw 接入：本地插件方式
- AI 接入：OpenAI 兼容接口

## 9. AI 在系统中的作用

AI 不是用户直接操作的对象，而是系统内部的智能规划层。

它主要负责：

- 分析用户命令
- 理解需求意图
- 在候选 Skill 中挑选更合适的组合
- 生成更自然的工作流说明和使用步骤

如果 AI 没有启用，系统仍然可以使用本地规则完成规划。

## 10. AI 接入端口

当前 AI 接入方式是标准 OpenAI 兼容接口。

默认接口地址：

```text
https://api.openai.com/v1/chat/completions
```

在项目里，这部分由后端统一调用，用户和聊天窗口不会直接访问这个接口。

## 11. OpenClaw 接入方式

当前设计不是“让用户离开聊天页面去访问 FlowHub”，而是：

- OpenClaw 聊天是唯一外部入口
- FlowHub 是聊天背后的规划后端
- 用户看到的一切都尽量停留在对话窗口里

这意味着：

- 用户体验更自然
- 对新手更友好
- 更符合 OpenClaw 的对话式机制

## 12. 用户能看到什么

用户在聊天中通常会看到以下内容：

- 需求已被理解
- 系统选中了哪些 Skill
- 每个 Skill 是做什么的
- 工作流会怎样执行
- 输出结果会是什么格式
- 是否需要确认继续

确认后，用户还能继续收到：

- 请求已进入待执行状态
- 面向客户或最终用户的回复内容
- 后续执行反馈

## 13. 当前已经实现的能力

- ClawHub Skill 目录同步
- 自然语言转工作流规划
- Skill 组合推荐
- 用户确认前后的消息生成
- OpenClaw 对话插件桥接
- 工作流执行 telemetry 回传
- 可选 AI 规划入口

## 14. 当前还在演进中的部分

- 更强的 AI 规划能力
- 更复杂的多 Skill 分支工作流
- 更完整的生产级执行调度
- OpenClaw 正式环境部署和运营化配置

## 15. 这个项目的价值

对用户来说：

- 不用理解 Skill 和 workflow 的技术细节
- 只需要会发消息、会确认

对业务来说：

- 可以快速把 OpenClaw 生态能力封装成对话式服务
- 可以把复杂自动化转成统一入口

对生态来说：

- 让更多非开发者真正用上 OpenClaw 的 Skill 能力

## 16. 一句话总结

FlowHub 的本质，是把 OpenClaw 的 Skill 生态从“开发者工具形态”转成“用户对话形态”。

