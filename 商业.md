# FlowHub 商业化参考方案

版本：`v0.1.0-draft`  
更新时间：`2026-03-11`

## 1. 文档目的

本文件不是当前版本的交付承诺，而是后续商业化升级时的参考文档。

当前决策：

- 现阶段先做免费版
- 暂不在产品里上线付费拦截和支付流程
- 保留后续“精品”能力，但先不做前台售卖逻辑

## 2. 当前阶段定位

FlowHub 当前阶段按免费版产品思路运行：

- 用户通过 OpenClaw Bot 对话发送自然语言需求
- 平台识别消息是否为有效需求
- 如果是有效需求，则从索引目录中检索可用 Skill 并生成一套可执行方案
- 如果不是有效需求，则直接返回正确使用方法

当前阶段不区分“优秀”和“精品”两个前台档位。

后续商业化版本只保留一个高阶名称：

- `精品`

## 3. 免费版规则

### 3.1 用户输入识别

平台先判断用户消息是否属于“有效需求”。

有效需求至少应具备以下要素中的两项以上：

- 明确任务动作
  - 例如：分析、抓取、整理、总结、导出、比较
- 明确对象
  - 例如：某个链接、某个 API、某组股票代码、某份文档
- 明确结果形式
  - 例如：Markdown 简报、表格、摘要、结论

以下内容默认视为非需求：

- 问候语
- 闲聊
- 只有一句模糊表述且没有对象
- 与平台能力明显无关的内容

### 3.2 非需求返回策略

若系统判断当前消息不是有效需求，则不进入工作流规划。

统一返回：

- 简短说明当前消息无法生成工作流
- 告诉用户正确使用方式
- 给出 2-3 条可直接复制的示例命令

示例返回策略：

```text
这条消息还不足以生成工作流。
你可以这样提需求：
1. 分析 AAPL 和 NVDA 最近 3 个月走势，并给我一份 markdown 简报
2. 抓取这个接口的数据并导出 csv
3. 总结这篇文章的核心观点并输出给客户
```

### 3.3 免费版规划策略

免费版只追求“可完成”和“低摩擦”，不追求最复杂的工作流。

原则：

- 优先选择能独立完成任务的单个 Skill
- 如果单个 Skill 无法合理完成，则生成最小可行方案
- 免费版输出以“普通可用结果”为目标
- 不主动生成深度分析型高成本方案

免费版的目标不是展示平台全部能力，而是让用户快速感知：

- 平台理解需求
- 平台能自动选 Skill
- 平台能给出结果

## 4. 精品版定义

后续商业化版本不再按 Skill 个数区分“优秀”和“精品”。

仅保留一个高阶档位：

- `精品`

`精品` 的判定标准改为“是否需要深度分析与更高算力成本”，而不是“用了几个 Skill”。

也就是说：

- 可能是 2 个 Skill
- 也可能是 6 个 Skill
- 个数不作为前台售卖标准

真正决定是否属于精品的因素包括：

- 任务复杂度
- 跨数据源整合深度
- 分析推理成本
- 是否需要更长上下文
- 是否需要更高质量结果输出
- 是否需要更强的结构化交付

## 5. 精品版前台展示原则

在免费版阶段，不向用户开放完整精品内容。

后续若升级商业化版本，前台只做以下提示：

- 当前需求可升级为 `精品方案`
- 精品方案将进行更深入的分析和更高质量编排
- 支付后可查看完整内容
- 支付后可下载交付文件

支付前不展示：

- 完整 Skill 列表
- 完整 workflow 节点结构
- 完整执行细节
- 全量分析结论

支付前只展示：

- 这是普通方案还是精品方案
- 该方案的预期价值
- 该方案适合解决什么问题

## 6. 订阅策略草案

按本轮确认，后续商业化可先采用如下简化方案：

- 免费用户：仅使用免费版能力
- 订阅用户：开放 `精品请求额度`

暂定订阅口径：

- 月订阅用户
- 季订阅用户
- 年订阅用户

当前参考规则：

- 每个订阅用户开放 `20 次精品组合请求`

说明：

- 此规则先按你的要求记录为统一参考口径
- 正式上线前，需要再按真实算力成本、转化率和复购情况校正
- 如果后续发现月订、季订、年订统一给 20 次在经济上不合理，再拆分为不同额度

## 7. 商业化成本控制建议

精品请求本质上是高成本能力，必须做额度管理。

建议后续商业化版本增加以下控制项：

- 每个用户的精品请求总额度
- 每次精品请求的消耗记录
- 按用户、按周期的配额账本
- 超额后的拦截与续费提示
- 高风险或高耗时请求的二次确认

否则会出现：

- 算力成本失控
- 单用户高频滥用
- 复杂任务把免费版拖成亏损模型

## 8. 推荐的产品行为

### 8.1 免费版

对话入口行为：

- 判断是否为需求
- 若不是需求，返回使用方法
- 若是需求，尝试生成普通方案
- 普通方案优先单 Skill 或最小可行组合
- 返回可读结果和下一步提示

### 8.2 后续精品版

对话入口行为：

- 先识别该请求是否属于深度分析场景
- 若属于精品场景，先提示可升级为 `精品`
- 未支付前，仅显示摘要提示
- 支付后，解锁完整方案与下载能力

## 9. 数据与权限设计建议

后续若进入商业化开发，建议新增以下对象：

- `user_accounts`
  - 用户身份、渠道来源、套餐状态
- `subscription_plans`
  - 免费、月订、季订、年订
- `subscription_entitlements`
  - 每个用户当前可用的精品额度
- `payment_orders`
  - 支付订单和支付状态
- `quota_usage_logs`
  - 每次精品请求的额度扣减记录
- `run_request_access_level`
  - 当前请求属于免费还是精品

## 10. 当前版本与后续版本边界

### 当前版本先做

- 免费版入口
- 非需求识别
- 正确使用方法返回
- 普通需求工作流生成
- 用户可读结果返回

### 当前版本不做

- 支付
- 订阅
- 精品权限控制
- 精品下载解锁
- 额度扣减

### 后续商业化版本再做

- 精品判定
- 套餐与订阅体系
- 配额与扣费
- 支付后解锁完整方案
- 下载与交付文件输出

## 11. 实施建议顺序

建议后续按以下顺序推进，而不是直接上支付：

1. 先补“非需求识别”和“正确使用方法返回”
2. 再把免费版规划策略收敛到“单 Skill 优先、最小可行组合兜底”
3. 再加用户身份和套餐字段
4. 再做精品判定与精品预览
5. 最后接支付、额度和下载

## 12. 当前可行性结论

基于现有项目状态，该商业化方向是可行的。

原因：

- 对话入口已经具备
- 自然语言转 Skill/Workflow 的核心能力已经具备
- 本地索引目录检索已经具备
- 计划与确认链路已经具备

当前最大差距不在“能不能生成工作流”，而在：

- 如何识别非需求
- 如何定义免费与精品边界
- 如何保护高成本能力不被免费滥用
- 如何把权限、额度和支付串起来

## 13. 参考结论

本文件确认的产品方向如下：

- 当前版本先做免费版
- 前台取消“优秀”
- 保留“精品”作为后续高阶商业化名称
- 精品不再按 Skill 数量定义
- 精品改为按深度分析与可行性判断定义
- 订阅用户后续开放 20 次精品组合请求
- 支付前仅提示有精品可选
- 支付后才开放完整查看与下载

本文件作为后续商业化升级的参考文档使用。
