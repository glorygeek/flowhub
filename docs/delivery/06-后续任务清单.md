# FlowHub 后续任务清单

更新时间：`2026-03-15`

## 任务表

| 优先级 | 状态 | 任务 | 目标 | 当前处理 |
| --- | --- | --- | --- | --- |
| P0 | 已完成 | OpenClaw 发现型 Skill 补真实搜索工具 | 让聊天侧能直接找可信 Skill，而不是只靠规划返回 | 已接 `flowhub_search_skills` |
| P1 | 已完成 | 客户端真实抓取 Skill 并本地组合执行 | 按 `selected_skills` 去官方中心抓取、缓存、组合执行 | 已支持官方 Registry 抓取、429 重试、缓存回退、resolved workflow 落盘 |
| P1 | 已完成 | 客户端真实 AI 执行替代模拟执行 | 用客户端模型或算力替代当前 `simulateNodeExecution` | 已接客户端 OpenAI 兼容运行时，支持读取 `client/.env` / `backend/.env`、DeepSeek thinking、失败回退 |
| P1 | 已完成 | Tauri 客户端打包与回归验收 | 完成 `deploy_03` 实际闭环 | 已补 Rust 工具链、图标资源，并实测产出 `exe/msi/nsis` |
| P2 | 已完成 | 前端正式部署验收 | 完成 `deploy_02` 正式交付闭环 | 已用 `docker compose` 生产编排启动并验证前端代理与后端健康链路 |
| P2 | 已完成 | 标签库第二阶段 | 建立可运营标签体系与人工修正能力 | 已落地 `tag_definitions` / `skill_tag_links`、自动派生标签、`/api/v1/skills/tags` 与 `tags=` 过滤 |
| P2 | 已完成 | 股票/A股等领域路由增强 | 减少跨领域误召回，提升中文金融场景稳定性 | 已加 A 股/美股/加密市场域加减权与回归测试 |
| P2 | 已完成 | 请求策略规则表配置化 | 将 API 抓取 / 客户回复等策略从代码常量抽成可运营规则 | 已落地 `search_policy_rules`、`/api/v1/skills/search/policies` 与搜索侧动态加减权 |
| P2 | 已完成 | 请求策略规则管理页 | 让运营侧直接调整规则启停、分值和优先级 | 已挂到 `/skills` 控制台并实测通过前端构建 |
| P2 | 已完成 | Skill 安全审查与标记 | 对索引 Skill 产出安全画像、红旗与操作建议 | 已落地 `skill_security`、`/api/v1/skills/{skill_id}/security-review`、`security:*` 标签与内部 `flowhub-skill-vetter` |
| P2 | 已完成 | Skill 人工安全复核写回 | 让运营能明确封锁/谨慎/放行某个 Skill 并影响默认规划 | 已落地 `PUT /api/v1/skills/{skill_id}/security-review`、`/security-history`、导出接口与默认规划拦截 |
| P2 | 已完成 | 标签修正历史与审批备注 | 让运营标签调整有历史记录和备注说明 | 已落地 `operator_change_logs`、`/skills/{skill_id}/tag-history` 与控制台历史面板 |
| P2 | 已完成 | 搜索策略历史与回滚 | 让策略规则可追溯并回退到历史状态 | 已落地 `/skills/search/policies/{rule_id}/history` 与 rollback 接口/控制台按钮 |
| P2 | 已完成 | 客户端外部工具调用沙箱 | 允许受限 HTTP / shell 工具执行并默认拒绝越权调用 | 已落地 `external_tool` 沙箱执行、Registry 抓取共用限制、未命中白名单主机直接拒绝 |
| P3 | 已完成 | 审计导出与告警机制 | 失败运行可导出、异常节点可聚合、可选 webhook 通知 | 已落地 `/run-requests/export`、`/telemetry/events/anomalies`、`/telemetry/events/export` 与 webhook 告警 |
| P3 | 已完成 | webhook 告警重试与投递日志 | 告警失败可自动重试，且每次运行有可审计投递记录 | 已落地 `audit_alert_deliveries`、`/telemetry/alerts`、导出接口和 `/runs` 面板 |
| P3 | 已完成 | webhook 告警手动重放 | 支持对单条告警投递手动重试并记录新日志 | 已落地 `/telemetry/alerts/{delivery_id}/replay` 与 `/runs` 面板按钮 |
| P3 | 已完成 | 内部维护学习流程落盘 | 将 `flowhub-self-improvement` 接到内部维护工作流 | 已补内部插件包 `openclaw-plugin/internal-maintenance` |
| P3 | 已完成 | 客户端 shell 白名单升级为签名工具清单 | 将 shell 执行从命令名白名单升级为路径+哈希约束 | 已落地 `CLIENT_TOOL_MANIFEST_*`、样例 manifest、SHA-256 校验与 smoke plan |

## 当前状态

- 当前主链路、客户端运行时、Tauri 工具链、部署验收与标签库第二阶段都已闭环。
- 剩余工作更适合归类为“优化项”，而不是部署阻塞项。

## 后续优化建议

1. 给客户端安装包与签名工具清单补正式发布流程，例如签名轮换、灰度分发和撤销台账。
2. 给 webhook 告警补更高层的运营模板，例如默认值继承、目的地分组、静默日历和跨环境策略。

## 备注

- 本清单现在同时记录“已闭环结果”和“后续优化项”，便于交付后继续迭代。
- 若某项完成，应同步更新 `README.md`、`docs/delivery/README.md` 和相关交付文档。
- 当前 Windows 构建机已补齐 Rust 工具链，`npm run tauri:build` 已通过。
